# What GoodData's MCP endpoint offers, measured

Generated by `gd-agents mcp-probe` against 4 workspace(s). Not written from memory — every number below came from a live call.

## Findings

- Every workspace advertises the same 3 tools (create_visualization, search_tools, call_tool). The endpoint is a gateway: the analytical tools are reached through `call_tool` by name, and `tools/list` does not name them. A caller that trusts `tools/list` to describe the surface will conclude GoodData's MCP can draw a chart and nothing else.
- `call_tool` dispatches by name with no `search_tools` call first, so restricting a lane to a named set of tools is a decision the *client* makes. The server does not offer a way to narrow what it will run.
- An unfiltered `list_workspace_metrics` costs up to ~43,299 tokens (globalmart-customer), and ~70,045 tokens across all 4 workspaces. A four-lane question that lists metrics before querying pays that before any reasoning begins — and it scales with how well modelled the workspace is, so it is worst for the customers who have invested most in their semantic layer.
- `ai_search` answers the same discovery need for ~1,358 tokens against ~70,045 — about 52x cheaper. So MCP's context cost is mostly a property of how well the client is written, not of the protocol. A caller who reaches for the obvious tool pays the large number; one who knows about `ai_search` does not. That is a documentation and defaults problem, and it is the single most actionable finding here.

## Cost per discovery route

| workspace | tool | approx tokens | ms |
|---|---|---:|---:|
| globalmart-customer | `get_workspace_info` | 23 | 340 |
| globalmart-customer | `ai_search` | 48 | 1342 |
| globalmart-customer | `list_workspace_metrics` | 43,299 | 943 |
| globalmart-customer | `list_workspace_attributes` | 11,800 | 685 |
| globalmart-marketing | `get_workspace_info` | 23 | 355 |
| globalmart-marketing | `ai_search` | 397 | 1156 |
| globalmart-marketing | `list_workspace_metrics` | 10,574 | 584 |
| globalmart-marketing | `list_workspace_attributes` | 9,716 | 577 |
| globalmart-store-ops | `get_workspace_info` | 25 | 334 |
| globalmart-store-ops | `ai_search` | 250 | 1035 |
| globalmart-store-ops | `list_workspace_metrics` | 13,804 | 697 |
| globalmart-store-ops | `list_workspace_attributes` | 9,096 | 566 |
| globalmart-ecommerce | `get_workspace_info` | 24 | 345 |
| globalmart-ecommerce | `ai_search` | 663 | 1183 |
| globalmart-ecommerce | `list_workspace_metrics` | 2,368 | 462 |
| globalmart-ecommerce | `list_workspace_attributes` | 6,824 | 560 |
