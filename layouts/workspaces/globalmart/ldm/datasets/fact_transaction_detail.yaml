aggregatedFacts: []
attributes:
- description: Unique identifier for each transaction in the Transaction Detail dataset, used to filter, group, or drill into
    analysis at the individual transaction level.
  id: fact_transaction_detail.txn_id
  labels: []
  sourceColumn: txn_id
  sourceColumnDataType: STRING
  tags:
  - Transaction Detail
  title: Transaction ID (Transaction Detail)
dataSourceTableId:
  dataSourceId: '{{ datasource_id }}'
  id: fact_transaction_detail
  path:
  - '{{ datasource_schema }}'
  - fact_transaction_detail
  type: dataSource
description: Transaction Detail
facts:
- description: Monetary amount recorded for each transaction detail line in the Transaction Detail dataset, used for summing
    and analyzing transaction value.
  id: fact_transaction_detail.amount
  sourceColumn: amount
  sourceColumnDataType: NUMERIC
  tags:
  - Transaction Detail
  title: Amount (Transaction Detail)
grain:
- id: fact_transaction_detail.txn_id
  type: attribute
id: fact_transaction_detail
references:
- identifier:
    id: dim_store
    type: dataset
  multivalue: false
  sources:
  - column: store_id
    dataType: STRING
    target:
      id: dim_store.store_id
      type: attribute
- identifier:
    id: transaction_date
    type: dataset
  multivalue: false
  sources:
  - column: txn_date
    dataType: DATE
    target:
      id: transaction_date
      type: date
tags:
- Transaction Detail
title: Transaction Detail
workspaceDataFilterReferences: []
