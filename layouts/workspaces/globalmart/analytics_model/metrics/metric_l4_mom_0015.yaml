content:
  format: '#,##0.00'
  maql: SELECT {metric/metric_l3_0005_0} - (SELECT {metric/metric_l3_0005_0} FOR PREVIOUS ({label/transaction_date.month}))
description: Tracks the month-over-month change in total return amount for the In-Store channel by comparing the current month’s
  in-store returns to the prior month. Use it to monitor whether in-store return value is rising or falling over time at a
  monthly level.
id: metric_l4_mom_0015
tags:
- L4
title: 'MoM: Return Amount (Returns) — In-Store Channel'
