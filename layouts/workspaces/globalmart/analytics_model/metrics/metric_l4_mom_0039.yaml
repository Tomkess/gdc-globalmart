content:
  format: '#,##0.00'
  maql: SELECT {metric/metric_l3_0013_0} - (SELECT {metric/metric_l3_0013_0} FOR PREVIOUS ({label/transaction_date.month}))
description: Shows the month-over-month change in the number of in-store gift card transactions by comparing the current month’s
  count to the prior month. Use it to monitor whether in-store gift card transaction volume is rising or falling over time.
id: metric_l4_mom_0039
tags:
- L4
title: 'MoM: Transaction Count (Gift Card Transaction) — In-Store Channel'
