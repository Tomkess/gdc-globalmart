content:
  format: '#,##0.00'
  maql: SELECT ({metric/metric_l3_0064_0} - (SELECT {metric/metric_l3_0064_0} FOR PREVIOUS ({label/transaction_date.year})))
    / (SELECT {metric/metric_l3_0064_0} FOR PREVIOUS ({label/transaction_date.year}))
description: Measures the year-over-year percentage change in Days of Cover for the In-Store sales channel, comparing the
  current period’s inventory coverage to the same metric in the prior year. Use it to track whether in-store inventory is
  lasting longer or shorter than last year within the selected reporting scope.
id: metric_l4_yoy_0192
tags:
- L4
title: 'YoY: Days of Cover (Inventory Health) — In-Store Channel'
