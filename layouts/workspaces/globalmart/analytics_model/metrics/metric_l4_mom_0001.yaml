content:
  format: '#,##0.00'
  maql: SELECT {metric/metric_l3_0000_1} - (SELECT {metric/metric_l3_0000_1} FOR PREVIOUS ({label/transaction_date.month}))
description: Shows the month-over-month change in the number of orders placed through the Online channel by comparing the
  current month’s order count to the prior month. Use it to monitor whether online order volume is trending up or down over
  time at a Month/Year level.
id: metric_l4_mom_0001
tags:
- L4
title: 'MoM: Order Count — Online Channel'
