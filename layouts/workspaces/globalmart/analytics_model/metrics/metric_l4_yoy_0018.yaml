content:
  format: '#,##0.00'
  maql: SELECT ({metric/metric_l3_0006_0} - (SELECT {metric/metric_l3_0006_0} FOR PREVIOUS ({label/transaction_date.year})))
    / (SELECT {metric/metric_l3_0006_0} FOR PREVIOUS ({label/transaction_date.year}))
description: Shows the year-over-year percentage change in the number of units returned through the In-Store sales channel,
  comparing the current year with the prior year. Use it to track whether in-store return volume is increasing or decreasing
  over time.
id: metric_l4_yoy_0018
tags:
- L4
title: 'YoY: Quantity Returned (Returns) — In-Store Channel'
